#!/bin/bash

#PBS -l nodes=2:ppn=16
#PBS -l walltime=00:05:00
#PBS -q workq
#PBS -N hellof
#PBS -j oe
#PBS -V

cd $PBS_O_WORKDIR
cat $PBS_NODEFILE |uniq > hosts

mpirun -npernode 4 -hostfile hosts ./a.out

